# Core dependencies for web app (minimal for smaller image)
flask==3.0.0
numpy==2.0.2
gunicorn==21.2.0

# PyTorch CPU-only version (much smaller than GPU version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu